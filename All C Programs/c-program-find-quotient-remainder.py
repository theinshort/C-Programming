/* * C Program to find the quotient and remainder */ #include <stdio.h> int main(){    int a,b;    printf("Enter Dividend: ");    scanf("%d",&a);    printf("Enter Divisor: ");    scanf("%d",&b);     // Calculates the quotient    int quotient = a/b;     // Calculates the remainder    int remainder = a%b;    printf("Quotient is : %d\nRemainder is: %d",quotient,remainder);    return 0;}