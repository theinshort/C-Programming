/* * C program to convert the given binary number into decimal using array */#include <stdio.h>#include <math.h>int main(){    int number;    int arr[100]; //declaration of array    int sum = 0;    for (int i = 0; i < 100; i++)    {        arr[i] = 0; //intializing array with 0    }     int i = 0;    printf("Enter the number: ");    scanf("%d", &number);    while (number > 0)    {        //storing ith index as (i+1)th bit*2^(i);        arr[i] = (number % 10) * pow(2, i);         number /= 10;        i++;    }    for (int i = 0; i < 100; i++)    {        //summation of all the individual (i+1)th bit*2^(i)        sum += arr[i];     }     printf("The Decimal Value of Binary Number is %d", sum);    return 0;}