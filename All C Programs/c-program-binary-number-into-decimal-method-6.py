/* * C program to convert the given binary number into decimal using array */#include <stdio.h>#include <math.h>int main(){    int size;    int sum = 0;     printf("Enter the size of binary number: ");    scanf("%d", &size);     int binary[size];    printf("Enter the binary number: ");     for (int i = 0; i < size; i++)    {        scanf("%d", &binary[i]);    }     for (int i = 0; i < size; i++)    {        sum += binary[size - i - 1] * pow(2, i);    }     printf("The Decimal Value of Binary Number is %d", sum);    return 0;}