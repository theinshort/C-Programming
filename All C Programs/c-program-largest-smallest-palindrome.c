/* * C Program to Find the Largest & Smallest possible  * Word which is a Palindrome */#include <stdio.h>#include <string.h>#include <ctype.h> int palin_check(char []); int main(){    char string[100], word[20], max[20], min[20], c;    int i = 0, j = 0, flag = 0, check;     printf("Enter string: ");    i = 0;    do    {        fflush(stdin);        c = getchar();        string[i++] = c;     } while (c != '\n');    string[i - 1] = '\0';    for (i = 0; i < strlen(string); i++)    {        while (i < strlen(string) && !isspace(string[i]) && isalnum(string[i]))        {            word[j++] = string[i++];        }        if (j != 0)        {            word[j] = '\0';            check = palin_check(word);            if (check)            {                if (!flag)                {                    flag = !flag;                    strcpy(max, word);                    strcpy(min, word);                }                if (strlen(word) > strlen(max))                {                    strcpy(max, word);                }                if (strlen(word) < strlen(min))                {                    strcpy(min, word);                }            }            j = 0;        }    }    if (flag)    {        printf("The largest palindrome is '%s' and smallest palindrome is '%s' in '%s'.\n", max, min, string);    }    else    {        printf("No palindrome words exists in '%s'.\n", string);    }     return 0;} int palin_check(char str[]){    int i, len;     len = strlen(str);    for (i = 0; i < len / 2; i++)    {        if (tolower(str[i]) != tolower(str[len - (i + 1)]))        {            return 0;        }    }     return 1;}