/* * C Program to Display ASCII value of all Characters used in the String */ #include <stdio.h>#include <string.h>#include <ctype.h> struct detail{    char c;    int ascii;}; int update(struct detail [], const char [], int); int main(){    struct detail s[26];    char string[100], c;    int i = 0, index;     for (i = 0; i < 26; i++)    {       s[i].c = i + 'a';       s[i].ascii = 0;    }    printf("Enter string: ");    i = 0;    do    {        fflush(stdin);        c = getchar();        string[i++] = c;        if (c == '\n')        {            break;        }        index = c - 'a';        s[index].ascii = c;    }    while (1);    string[i - 1] = '\0';    printf("The string entered is: %s\n", string);     printf("*************************\nCharacter\tASCII\n*************************\n");    for (i = 0; i < 26; i++)    {        if (s[i].ascii)        {            printf("     %c\t\t   %d\n", s[i].c, s[i].ascii);        }    }     return 0;}