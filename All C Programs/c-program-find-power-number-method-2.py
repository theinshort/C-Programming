/* * C program to find power of any number using for loop */ #include <stdio.h> int main(){    int base, exponent;    float power = 1;    int i;     /* Take base and exponent as input*/    printf("Enter base: ");    scanf("%d", &base);    printf("Enter exponent: ");    scanf("%d", &exponent);    int expo = exponent;     //To calculate the power of negative exponents    for(i=expo;i<0;i++)    {        power = power/base;    }     //To calculate the power of positive exponents    for(i=expo;i>0;i--)    {        power = power * base;    }    printf("%d ^ %d = %f", base, exponent, power);     return 0;}