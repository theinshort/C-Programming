/* * C program to find power of any number using recursion */#include <stdio.h> float power (int, int);float negpower (int,int); int main(){    int pow, num;    float result;     printf("Enter a number: ");    scanf("%d", &num);    printf("Enter it's power: ");    scanf("%d", &pow);    if (pow<0)        result = 1/negpower(num, pow);    else        result = power(num, pow);    printf("%d^%d is %f", num, pow, result);    return 0;} //function to find power if base is negativefloat negpower(int num,int pow){    if (pow<0)    {        return (num * negpower(num, pow + 1));    }    return 1;} //function to find power if base is positivefloat power (int num, int pow){    if (pow)    {        return (num * power(num, pow - 1));    }    return 1;}