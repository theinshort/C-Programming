/* * C program to convert the given binary number into decimal using functions */#include <stdio.h>#include <math.h> //Function to convert Binary to Decimalint Binary_to_Decimal(int number){    int sum = 0; int i = 0;    while (number > 0)    {        sum += (number % 10) * pow(2, i);        number /= 10;        i++;    }    return sum;} //Driver Codeint main(){    int number;     printf("Enter the number: ");    scanf("%d", &number);     printf("The Decimal Value of Binary Number is %d", Binary_to_Decimal(number));    return 0;}