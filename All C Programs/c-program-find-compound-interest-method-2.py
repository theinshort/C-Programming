/* * C Program to calculate compound interest using function */ #include <stdio.h>#include <math.h> void Compound_Interest(double Principal, double Rate, double Time){    // Calculating compound interest    double Amount;    Amount = Principal * pow((1 + Rate / 100), Time);     double Compound_Interest;    Compound_Interest = Amount - Principal;     printf("The Compound Interest is %.2lf ", Compound_Interest);} int main(){    double Principal;    printf("Enter the Principal Amount: ");    scanf("%lf", &Principal);     double Rate;    printf("Enter the Interest Rate: ");    scanf("%lf", &Rate);     double Time;    printf("Enter the Time Period(in Years): ");    scanf("%lf", &Time);     Compound_Interest(Principal, Rate, Time);     return 0;}