/* * C Program to find the quotient and remainder using loops */ #include <stdio.h> int main(){    int a,b;    printf("Enter Dividend: ");    scanf("%d",&a);    printf("Enter Divisor: ");    scanf("%d",&b);    int quotient = 0;     //Initializing the loop    while(quotient*b <= a)      quotient++;       //Decrementing 1 because we came one step forward      quotient--;      int remainder= a - (b * quotient);     printf("Quotient is : %d\nRemainder is: %d",quotient,remainder);    return 0;}